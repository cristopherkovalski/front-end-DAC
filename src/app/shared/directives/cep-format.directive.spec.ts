import { CepFormatDirective } from './cep-format.directive';

describe('CepFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CepFormatDirective();
    expect(directive).toBeTruthy();
  });
});

import { TelefoneFormatDirective } from './telefone-format.directive';

describe('TelefoneFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new TelefoneFormatDirective();
    expect(directive).toBeTruthy();
  });
});
